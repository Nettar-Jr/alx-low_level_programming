#include "main.h"

/**
 * main - Entry point
 *
 * Retrun: Always 0 (success)
 *
 */
int main(n)
{
	char alpha;

	for (alpha = a; alpha <= z; alpha++)
	{
		print_alphabet(alpha);
	}

	print_alphabet('\n');
	return (0);
}
