#include "main.h"

/**
 * _abs - compute the absolute value of an int
 *
 * @r : letter to print
 *
 * Return: Always 0
 */
int _abs(int r)
{
	if (r)
		return (1);
	else
		return (0);

	return (0);
}
