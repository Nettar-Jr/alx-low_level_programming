#include "main.h"
#include <string.h>

/**
 * _strcpy - copy element from one array to another
 * @src : array source
 * @dest : array destination
 *
 * Return: 0
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

	return (dest);
}
